import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    mapContainer: {
        height: "50%"
    },
    bottomContainer: {
        height: "50%"
    }
});

export default styles;
